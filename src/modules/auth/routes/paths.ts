export const PATH = {
  LOGIN: '/login',
  REGISTER: '/register',
  REPO: '/repositories',
  PULLS: '/repositories/:id/pull-requests',
  COMMIT: '/repositories/:id/pull-requests/:commitId',
  HOME : '/'
}
