export const PATH = {
  ROOT: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  HOME: '/home',
  REPO: '/repositories',
  PULLS: '/repositories/:id/pulls',
  COMMIT: '/repositories/:id/pulls/commit/:commitSHA',
}
